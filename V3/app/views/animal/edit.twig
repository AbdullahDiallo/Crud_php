
<!DOCTYPE html>
<html >

<head>
    <title>Modifier les infos d'un animal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/style/style.css">
</head>

<body>

<div class="container-form bg-white card card-body" style="max-width:480px; margin:0 auto; margin-top:110px;color:#000;">
         <h3>Modifier les info d'un Animal</h3>
        <form action="/update/{{ animal.getId() }}" method="POST">
         <div class="mb-3">
          <label for="animal-type">Type:</label>
            <input type="text" id="animal-type" class="form-control" name="type" value="{{ animal.getType() }}" required>
</div>
             <div class="mb-3">
           <label for="animal-age">Âge:</label>
            <input type="text" class="form-control" id="animal-age" name="age" value="{{ animal.getAge() }}" required>
</div>
             <div class="mb-3">
              <label for="animal-sante">Santé:</label>
            <input type="text" id="animal-sante" class="form-control" name="sante" value="{{ animal.getSante() }}" required>
</div>
           
<div class="mb-3">
    <label for="animal-id_equipement">Sélectionnez un équipement:</label>
    <select id="animal-id_equipement" name="id_equipement" class="form-control" required>
        <option value="">-- Sélectionnez un équipement --</option>
        {% for equipment in equipments %}
            <option value="{{ equipment.id }}" {% if equipment.id == animal.id_equipement %}selected{% endif %}>
                {{ equipment.nom }}
            </option>
        {% endfor %}
    </select>
</div>



            
            <button type="submit" class="btn-submit">Valider</button>
             <a class="btn-cancel" href="/animal">Retour à la liste</a>

        </form>
    </div>
    <script src="/js/valid.js"></script>
</body>
</html>