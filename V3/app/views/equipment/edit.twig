<!DOCTYPE html>
<html>

<head>
    <title>Modifier un Équipement</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/style/style.css">
</head>

<body>

<div class="container-form bg-white card card-body" style="max-width:480px; margin:0 auto; margin-top:110px;color:#000;">
         <h3>Modifier un équipement</h3>
        <form action="/equipment/update/{{ equipment.id }}" method="POST">
         <div class="mb-3">
            <label for="equipment-nom">Nom:</label>
            <input type="text" class="form-control" id="equipment-nom" name="nom" value="{{ equipment.nom }}" required>
         </div>
         <div class="mb-3">
            <label for="equipment-etat">État:</label>
            <input type="text" class="form-control" id="equipment-etat" name="etat" value="{{ equipment.etat }}" required>
         </div>
         <div class="mb-3">
            <label for="equipment-disponibilite">Disponibilité:</label>
            <select id="equipment-disponibilite" name="disponibilite" class="form-control" required>
                <option value="1" {% if equipment.disponibilite %}selected{% endif %}>Disponible</option>
                <option value="0" {% if not equipment.disponibilite %}selected{% endif %}>Indisponible</option>
            </select>
         </div>
            
            <button type="submit" class="btn btn-submit">Valider</button>
             <a class="btn btn-cancel" href="/equipment">Retour à la liste</a>
        </form>
    </div>
</body>
 <script src="/js/validation.js"></script>
</html>
